server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Samuel-910/DAD2024-G1.git # ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: main

  security:
    user:
      password: 123456
      name: root